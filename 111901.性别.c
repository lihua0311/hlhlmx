#include<stdio.h>
void main()
{
    int x;
    printf("输入:");
    scanf("%d",&x);
    if(x>=1&&x<=1)
    {
        printf("男");
    }
    else if(x<=0&&x>=0)
    {
        printf("女");
    }
    else{}
}
